# Production Readiness Plan — Launch Today# Problem# All anonymous/unauthenticated users share a single `demo-user` identity (progress, enrollments, certificates mixed together), and missing error/404/loading pages show ugly Next.js defaults.## Changes### 1. Unique anonymous user ID (9 files)Replace the `demo-user` fallback with an auto-generated UUID stored in `localStorage`. On first visit, generate `crypto.randomUUID()` and persist it as `biodockify_user_id`. All 9 files already read this key — they'll just get a real unique ID instead of `demo-user`.Files to change (same pattern in each — replace the fallback):`app/courses/[id]/page.tsx` line 21-22`app/courses/[id]/lessons/[lessonId]/page.tsx` line 34-35`app/courses/[id]/lessons/[lessonId]/practice/page.tsx` line 38`app/dashboard/page.tsx` line 14-15`app/certificates/[id]/page.tsx` line 15`app/brain/page.tsx` line 16`app/knowledge/page.tsx` line 12`app/cohorts/page.tsx` line 13 (fallback `admin` → UUID)`app/cohorts/[id]/page.tsx` line 18 (fallback `admin` → UUID)`### 2. Branded 404 page — `app/not-found.tsx`Simple page with the site's emerald/teal theme, 